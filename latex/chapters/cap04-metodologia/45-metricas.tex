\fasePlaceholder{Fase F --- Tabla 8: definición operativa de S, E, C y \AlignmentScore{}}

\CoverageScore{} (C) se descompone en proporciones de cobertura sobre dominio de servicio,
registro de control, \textit{behavior qualifiers}, operaciones y esquemas (\Cref{fig:proc-cobertura}).
El modelo oficial del plan integra solo S, E y C en \AlignmentScore{}
($\alpha S + \beta E + \gamma C$, con $\alpha + \beta + \gamma = 1$).

\begin{figure}[H]
  \centering
  \figApoyoC
  \caption{Desglose de \CoverageScore{} (C): componentes SD, CR, BQ, OP y SC. Fuente: elaboración
  propia. \textbf{Nota:} el panel inferior del diagrama original propone
  \textit{SchemaConformanceScore} ($\delta$); ese término \textbf{no} forma parte del modelo del
  plan de tesis (Guía 1) y no se incluye en la fórmula de \AlignmentScore{} de este documento.}
  \label{fig:proc-cobertura}
\end{figure}
